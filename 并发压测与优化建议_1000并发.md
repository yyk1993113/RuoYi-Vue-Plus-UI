# 并发压测与优化建议（1000 并发）

> 范围：B 端接口 + 小程序接口（本仓库视角）  
> 约束：不改代码，仅给出压测方案与优化建议

## 1. 结论性说明

当前前端仓库（RuoYi-Vue-Plus-UI）中能明确识别到的为**B端管理 API**为主。  
我在仓库中未直接检索到明确的“小程序专用前缀（如 /wx/ /mini/ /applet/ /mp/）”接口定义，故：

1. 下面给出 `B端接口` 的可压测列表（可直接执行）。  
2. 小程序接口先给出“接口清单待确认模板”，你补齐网关前缀或具体接口后即能直接复用。

## 2. 目标与验收阈值

### 2.1 核心目标

1. 在 `1000 并发`（steady-state）下验证服务可用性  
2. 评估并发下延迟、错误率、QPS、资源上限  
3. 给出不改代码条件下可先落地的优化动作

### 2.2 建议验收阈值（建议）

1. `http_req_failed` < 1%  
2. `p(95)` < 800ms，`p(99)` < 2000ms（不同接口可分级）  
3. 错误码分布可接受：`5xx` 持续低于 0.2%  
4. 服务器可复现指标：CPU < 75%、内存无持续上升、数据库连接池无持续打满  
5. 重要接口在 1000 并发下仍具备降级/保护（429/熔断/限流）行为，而非直连雪崩

## 3. 压测入口与准备

### 3.1 测试地址约定

1. 开发环境：`/dev-api`（项目默认）  
2. 生产环境：`/prod-api`（你给出的 Nginx 已配置反代）  
3. 建议正式压测以生产网关或预发布网关打点，且至少带 `Authorization` Token

### 3.2 鉴权准备

1. 登录接口：`POST /auth/login`（可先批量生成不同账号 token）  
2. 压测脚本中建议按 token 池轮询，避免单 Token 被限流  

## 4. 可压测接口清单（先执行）

> 以下路径均已在仓库 API 中确认存在；请求路径请按实际网关再加前缀（如 `/prod-api`）。

### 4.1 B端高频读接口（建议作为主压测）

1. `GET /system/user/list`  
2. `GET /system/role/list`  
3. `GET /system/dept/list`  
4. `GET /system/post/list`  
5. `GET /system/user/profile`  
6. `GET /admin/recruitment/company/list`  
7. `GET /admin/recruitment/job/list`  
8. `GET /admin/recruitment/apply/list`  
9. `GET /admin/recruitment/apply2/list`  
10. `GET /admin/recruitment/dashboard/overview`  
11. `GET /admin/recruitment/dashboard/applyTrend?days=7`  
12. `GET /workflow/task/pageByTaskWait`  
13. `GET /workflow/instance/pageByRunning`  
14. `GET /monitor/operlog/list`  
15. `GET /monitor/online/list`

### 4.2 B端写接口（建议独立压测，再做混合）

1. `POST /system/role/changeStatus`  
2. `POST /system/user/changeStatus`  
3. `POST /admin/content/banner/changeStatus`  
4. `POST /admin/content/course/changeStatus`  
5. `POST /admin/recruitment/company/silence`  
6. `POST /admin/recruitment/company/unsilence`  
7. `POST /workflow/task/completeTask`  
8. `POST /workflow/task/backProcess`  
9. `POST /admin/recruitment/job/changeStatus`  
10. `POST /admin/recruitment/apply/markRead`  
11. `POST /monitor/cache/clearCacheName/{cacheName}`（谨慎，可在非生产或灰度环境压测）

### 4.3 小程序接口（待确认）

1. 需要确认小程序网关前缀（例如 `/prod-api/mp`、`/prod-api/wechat` 等）  
2. 需补齐 10-20 个常用小程序读写接口，形成：
   1. `GET /wx/xxx/list`（列表）  
   2. `GET /wx/xxx/detail/{id}`（详情）  
   3. `POST /wx/xxx/action`（行为类）  

## 5. 压测脚本示例（k6）

### 5.1 基础 k6 脚本（复制后直接改路径）

```javascript
import http from 'k6/http';
import { check, sleep } from 'k6';
import { randomItem, randomIntBetween } from 'https://jslib.k6.io/k6-utils/1.2.0/index.js';

const API = __ENV.BASE_URL || 'http://admin.zgypzp.com/prod-api';
const token = __ENV.AUTH_TOKEN || '';
const headers = { Authorization: token ? `Bearer ${token}` : '', 'Content-Type': 'application/json' };

const bRead = [
  '/system/user/list?pageNum=1&pageSize=20',
  '/system/role/list',
  '/system/dept/list',
  '/admin/recruitment/company/list?pageNum=1&pageSize=20',
  '/admin/recruitment/job/list?pageNum=1&pageSize=20',
  '/admin/recruitment/apply2/list?pageNum=1&pageSize=20',
  '/workflow/task/pageByTaskWait?pageNum=1&pageSize=20',
  '/workflow/instance/pageByRunning?pageNum=1&pageSize=20',
  '/monitor/operlog/list?pageNum=1&pageSize=20'
];

const bWrite = [
  { method: 'POST', url: '/admin/content/banner/changeStatus', body: JSON.stringify({ bannerId: 1, status: '0' }) },
  { method: 'POST', url: '/admin/recruitment/company/silence', body: JSON.stringify({ companyIds: [1], reason: 'stress-test' }) },
  { method: 'POST', url: '/workflow/task/urgeTask', body: JSON.stringify({ taskId: 1 }) }
];

export const options = {
  stages: [
    { duration: '1m', target: 200 },
    { duration: '1m', target: 1000 },
    { duration: '3m', target: 1000 },
    { duration: '1m', target: 200 },
    { duration: '1m', target: 0 }
  ],
  thresholds: {
    http_req_failed: ['rate<0.01'],
    http_req_duration: ['p(95)<800', 'p(99)<2000'],
    checks: ['rate>0.99']
  }
};

export default function () {
  const base = `${API}${randomItem(bRead)}`;
  let readRes = http.get(base, { headers });
  check(readRes, { 'read ok': (r) => r.status === 200 || r.status === 304 });

  if (Math.random() < 0.3) {
    const req = randomItem(bWrite);
    const writeRes = http.post(`${API}${req.url}`, req.body, { headers });
    check(writeRes, { 'write ok': (r) => r.status === 200 || r.status === 204 });
  }

  sleep(randomIntBetween(0, 2) / 10);
}
```

### 5.2 运行命令（建议先跑读）

1. `k6 run --vus 1000 --duration 3m --env BASE_URL=http://admin.zgypzp.com/prod-api --env AUTH_TOKEN=Bearer_xxx ./loadtest.js`  
2. `k6 run --vus 1000 --duration 5m --rps 2000 --env BASE_URL=http://admin.zgypzp.com/prod-api --env AUTH_TOKEN=Bearer_xxx ./loadtest.js`（限流较严时调整）  

### 5.3 AB/WRK 替代方式（简易）

```bash
ab -n 60000 -c 1000 -H "Authorization: Bearer <token>" "http://admin.zgypzp.com/prod-api/system/user/list?pageNum=1&pageSize=20"
```

```bash
wrk -t8 -c1000 -d180s --latency http://admin.zgypzp.com/prod-api/system/user/list?pageNum=1&pageSize=20
```

> 注：ab/wrk 不支持复杂鉴权与场景混合，建议 k6/Locust 作为主工具。

## 6. 小程序接口压测建议（与 B 端分离）

1. 小程序端建议先抽 3 类：列表、详情、动作（提交）  
2. 同样按 `1000 并发` 做“列表 + 详情”混合，再加 20% 的写接口  
3. 小程序登录态通常是高频短链，建议独立 `token 池` 做压测，不要复用 B 端账号  
4. 如果小程序网关有签名/加解密，压测脚本需加入时间戳与签名生成逻辑（建议固定测试窗口）

## 7. 优化建议（无代码改造前优先执行）

### 7.1 立即可落地（高收益）

1. 在 Nginx/网关层做 `gzip`、`keepalive`、`proxy_connect_timeout`、`send/read timeout` 与连接池参数确认  
2. 对所有高频接口补齐合理的 `cache-control` 与后端层级缓存（字典、枚举、配置、列表维表）  
3. 对 `*list*` 接口统一分页上限（例如 pageSize 上限）  
4. 对 `GET` 类响应做字段裁剪，减少无效字段返回  
5. 为 `/monitor/*`、`/list*` 系列补充慢查询日志与索引监控  
6. 启用数据库慢查询日志门槛（例如 > 300ms）并对高频 SQL 建索引前置修正

### 7.2 风险较高但应尽快修

1. `login`、`auth code` 类接口增加严格限流（防止抖动和验证码风控误伤）  
2. `/auth/login` 与高频查询接口分离连接池与资源隔离，避免互相拖垮  
3. 统一幂等/防重放策略，减少短时间重复提交导致的数据库重复写  
4. 关键写接口添加幂等键（`request id` / 防重复签名）  
5. 异步化重任务（导出、批量状态变更）并立即返回任务ID  
6. B 端和小程序共用资源时设置更严格的优先级配额和隔离（租户/应用级限流）

### 7.3 中长期优化（建议立项）

1. 将列表查询改为“先查主表再分页批量加载关联”模式，避免大 JOIN + 大分页  
2. 对复杂统计接口（如 overview/dashboard）引入预聚合表或缓存层  
3. 为 workflow/task、apply、company/job 等关键链路补 Trace（链路追踪）  
4. 建立压测基线：固定数据量、固定时间窗、固定脚本版本，纳入 CI 夜间回归

## 8. 交付建议（执行顺序）

1. 第 1 步：先跑 B端只读 1000 并发（2-3组）  
2. 第 2 步：再跑 B端写接口（10% 写比）  
3. 第 3 步：再加混合场景（读 80% + 写 20%）  
4. 第 4 步：补齐小程序接口列表并同样执行  
5. 第 5 步：对异常接口逐个回归 + 优化，按“错误率/耗时”逆序修  

## 9. 结果记录模板（你可直接填）

1. 接口名  
2. 并发数  
3. 时长  
4. 平均时延 / p95 / p99  
5. 成功率 / 错误率（按 4xx / 5xx）  
6. QPS  
7. 服务端 CPU / 内存 / DB 连接池 / 慢查询  
8. 结论与优化项优先级  
