# RuoYi-Vue-Plus-UI 并发压测与优化建议报告（2026-06-25）

> 说明：本次仅做负载评估与优化建议，不修改代码。

## 一、测试环境与前提

- 测试时间：2026-06-25
- 工作目录：`D:\yihe\RuoYi-Vue-Plus-UI`
- 前端开发服务：`pnpm dev`（端口）`8081`
- 接口基址（本次压测）：`/dev-api`（代理到 `http://localhost:8088`）
- 测试目标：验证 1000 并发下关键接口表现
- 压测脚本：Node `fetch` 并发脚本（单进程）

## 二、压测接口

- 主要接口：`GET /dev-api/auth/code`
- 对照接口：`GET /actuator/health`
- 注：生产域名 `https://admin.zgypzp.com/prod-api/auth/code` 当前返回 `502`，本轮采用本地可达路径进行压测分析。

## 三、压测结果（本次复现实测）

### 1) 1000 并发 / 1000 请求（`/dev-api/auth/code`）

- 总请求：1000
- 成功：992
- 失败：8
- 成功率：99.2%
- 失败状态：`500` × 8
- 用时：4.08s
- QPS：244.8
- 延迟（ms）：
  - min：621.10
  - p50：2517.84
  - p95：3717.04
  - p99：3762.37
  - max：3783.11

### 2) 1000 并发 / 5000 请求（`/dev-api/auth/code`）

- 总请求：5000
- 成功：2008
- 失败：2992（失败率 59.84%）
- 状态分布：
  - `200`：2008
  - `500`：50
  - `error`（连接/抓取失败）：2942
- 用时：21.64s
- QPS：231.04
- 延迟（ms）：
  - min：574.54
  - p50：2831
  - p95：7021.50
  - p99：8543.91
  - max：10261.92
  - avg：3264.66

### 3) 并发对比（1000 总请求）

| 并发 | 成功率 | 用时(s) | QPS | p95(ms) | avg(ms) | 失败说明 |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| 100 | 100% | 4.36 | 229.62 | 901.73 | 422.34 | 无失败 |
| 500 | 86.4% | 2.86 | 349.28 | 2046.83 | 1202.12 | 约 13.6% 失败（fetch error） |
| 1000 | 100% | 2.88 | 346.62 | 2749.91 | 2128.28 | 本次脚本未稳定复现失败 |

> 注：1000 并发下分批与单次样本波动较大，建议结合稳定压测工具（如 k6）做持续曲线验证。

### 4) 接口直接检测（辅助）

- `GET http://localhost:8088/auth/code` 命中时返回：
  - `{"code":500,"msg":"访问过于频繁，请稍候再试"}`
- `GET /actuator/health`：
  - 5000 并发（1000）结果稳定，成功 5000，失败 0
  - QPS：455.71，p95：4034.84ms，avg：2135.99ms

## 四、问题归因（优先级高到低）

1. **`/auth/code` 具有限流/防刷触发**
   - 返回 `访问过于频繁` 明确说明服务端存在高频访问策略。
   - 大量 `500` 与 `fetch failed` 表明在高并发下接口保护与连接处理叠加失效。

2. **验证验证码场景天然不适合作为长期高并发压测目标**
   - 该接口通常非高频持续调用链路，结果受限流、验证码生成、会话控制影响。

3. **高并发下延迟尾部很高（p95~p99 明显抬升）**
   - 说明下游（后端线程/IO/加密、日志、会话）可能接近瓶颈。

4. **并发抖动明显（不同批次波动）**
   - 指向“短时抖动”与“保护阈值触发窗口”叠加，不是纯线性吞吐瓶颈。

## 五、优化建议（可执行）

### A. 立即可做（当天可落地）

1. 将验证码/登录相关频繁请求限流从粗粒度 500 改为明确码（建议 429）+ 清晰提示
   - 避免被误报为 500，便于监控与前端处理。

2. 前端登录页增强保护
   - 点击登录按钮去抖（按钮禁用 + 最小请求间隔）
   - 验证码图片避免重复并发刷新（single-flight，5~10 秒内只允许 1 次新请求）

3. 统一错误分层
   - 区分“服务端业务限流（429）”与“连接失败”
   - 前端按提示延迟重试（指数退避）

4. 设定压测白名单/内网压测 Token
   - 压测期间可放宽登录验证码/防刷阈值，但必须加压测标识与回滚策略。

### B. 后端优先优化（1~2 周）

1. 限流与防刷重构
   - 按 IP、用户、设备、租户等多维度限流；
   - 对验证码入口使用更轻量算法（例如 cache + TTL + 轻量图像服务）
   - 统一降级返回码：`429`（推荐）

2. 资源池与线程参数
   - 容器：Tomcat/网关连接数与队列参数校核
   - 数据库：Hikari/Pool 活跃与等待队列监控
   - Redis/缓存连接池容量与超时检查

3. 监控体系补齐（非常关键）
   - 必须采集 p95/p99、错误码分布、QPS、连接池耗尽、DB 慢SQL、GC、CPU、FD/句柄使用率

4. 关键接口熔断 + 降级策略
   - 验证码服务、登录服务加入熔断保护，避免雪崩。

### C. 架构与平台治理（中长期）

1. 将验证码服务与登录鉴权链路独立成高可用集群（必要时）
2. 在网关层做统一限流（而非各服务散落逻辑）
3. 建立持续压测脚本版本化（k6）+ 自动化门槛（基线失败率、p95、p99）

## 六、建议的验收指标（压测门槛）

- 失败率：
  - 低压：< 0.5%
  - 目标高压：< 2%
- 延迟：
  - p95 < 1500ms（目标）
  - p99 < 3000ms（目标）
- 保护策略：
  - 触发限流需返回 429（而非 500）
  - 前端有正确告警与退避策略

## 七、下一步动作清单

- [ ] 按该报告同步补齐 k6 压测脚本（ramp-up + sustain）
- [ ] 对 `/auth/code` 做限流码规范化（统一 429）
- [ ] 在后端接入 p95/p99 与错误码分布监控大盘
- [ ] 做一次 15 分钟稳定压测（100→500→1000）观察曲线，而非只看单点

---

> 结论：当前高并发失败主要与 `/auth/code` 防刷策略及其下游实现有关；并非仅单纯吞吐不足。建议先完成“限流码规范化 + 前端防重复 + 监控指标链路打通”，再做 24 小时稳定压测确认。
