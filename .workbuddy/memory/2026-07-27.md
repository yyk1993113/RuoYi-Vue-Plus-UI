# 2026-07-27

## 白名单详情弹窗资质材料编辑

- 在 `src/views/recruitment/sub-account/index.vue` 的白名单详情弹窗「企业资质详情」标签页中，增加资质材料修改能力。
- 修改入口（"修改材料"按钮）仅在申请状态为 `APPROVED`（审核通过）时展示；`PENDING`（待审核）和 `REJECTED`（已驳回）状态不展示编辑入口。
- 新增 detail 弹窗编辑态变量、`detailQualificationIds`、上传/移除/保存/取消逻辑，复用现有审核弹窗的材料处理与校验规则（纳税记录/社保证明二选一必填）。
- 验证：`pnpm lint:eslint:fix` 后 `pnpm lint:eslint` 通过；`vue-tsc --noEmit` 未引入新的类型错误（项目历史类型错误与本修改无关）。
